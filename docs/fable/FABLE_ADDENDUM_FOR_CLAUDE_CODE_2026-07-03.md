# FABLE REVIEW ADDENDUM — delta pass on the 2026-07-03 08:28 tree

Same consumption contract as the 07-02 findings doc: verify at your HEAD, re-derive, no cross-agent diffs. Scope = the ~20 drops landed since the 22:04 tree. Method: full-tree AST (1,283 files) + YAML clean; 113-test battery across the night's slices; F1 smoke re-run ×6 + full 7-chain walkthrough on this tree; symbol spot-checks; changelog cross-reads.

---

## 1 · Scoreboard — the incorporation was done right

Everything I handed over landed or was correctly adjudicated: **F1** re-derived faithfully (same helper, same predicate, own 10-test slice + wiring canary; **6/6 and 7/7 re-verified here**, matching your 9/9). **F2/F3/F5** in `fable-review-incorporation` exactly as specified, observations logged under `OBS.fable_review_2026_07_02`. **F6** fully batched with real guard classes — including the GCW-residue *unreachability* guard (option b) and the colorblind crit var. **Appendix C's space item** resolved at the strong end: `course anomaly` was **wired into live combat** (FORK-1=A), then break-it-passed and hardened same-day — the `already_rewarded` single-owner flag on `_handle_combat_end` is the right shape. **36th arc** resolved honestly as venue-shape novelty (see §5). And **F4 was correctly overturned**: you implemented my exact fix-shape, tested the premise, and proved the `_overview.yaml` files aren't AreaGeometry (missing `schema_version` — Tier-1b artifacts for a different map layer). My diagnosis was wrong; the design-fork logging is the right disposition. That's the no-phantom-claims discipline applied *to the reviewer* — keep doing that.

## 2 · Two calibration corrections (mine — and a method upgrade for both of us)

The **staged-cult ZERO-payout** was live when I reviewed hollow-sun tuning. I verified the distributor's gating, funnel routing, and relic era-cleanness — but never traced whether the **staged path ever wrote contribution points**, and it didn't; `_distribute_rewards` short-circuited on `total <= 0`. The existing tests hand-injected `contribs` — and so, effectively, did my review. F4 was the same failure class: I verified the files *existed*, not that they were *valid for the consumer*. Lesson, stated as a rule worth adopting in the invariant-auditor's checklist: **for any distributor-gated pipeline, audit the signal *producer* on every path variant, not the distributor** — presence ≠ wiring, existence ≠ validity. Pre-launch candidates for a one-pass signal-producer audit (each has a distributor gated on upstream accumulation): territory contest scoring, bounty split/claim, org payroll (you just fixed one leg), communal title-share, chain `slot_totals` (now guarded), grind daily-cap counters. Your break-it passes are structurally better at this than static review — for reward pipelines, treat them as the primary gate, not the confirmation step.

## 3 · 🔴 New finding: the Qwen default flipped ahead of its own landmine hardening

`ollama-model-env-driven` shipped **§1 of the swap spec only** — env-driven model strings, tier2 env, invariant docs (all correct) — but it changed the **default** to `qwen3.5:9b`, so on next boot all three `json_mode=True` call sites run against Qwen with **none of §§2–6 landed**:
- **§2 (highest priority):** ollama/ollama **#14645** — `format:"json"` silently ignored for qwen3.5 when thinking is off (the Small-series default). Affected: `engine/idle_queue.py:96` and `:372` (both bark-array tasks — failure mode is *silent pool starvation*, not an error) — `ai/intent_parser.py` is already covered by its pre-existing `_extract_json` (:217). Action: run the spec's one-curl check against Brian's installed Ollama version and record it; harden both idle_queue parses with a first-balanced-`[…]` extraction regardless (test cases in the spec).
- **§3/§4:** the provider options block still sends only `temperature`/`num_predict` — no `num_ctx`, `keep_alive`, or sampling passthrough. With the 9B + bundled mmproj on the 8GB card, per-lane `num_ctx` caps (barks 2048 / talk 4096 / summaries 8192) are the difference between fitting and thrashing; Ollama's model-default context for this tag will not be chosen with your VRAM in mind.
- **§5:** both bark tasks still hardcode `temperature=0.85` (Mistral-tuned; Qwen non-thinking recs are 0.7/top_p 0.8/top_k 20/presence_penalty 1.5, and the 9B has documented repetition tendencies without the penalty). Put the four in tunables per the spec.
- **§6/§7:** era-drop telemetry + the acceptance cycle (parse-rate ≥ baseline over one 4-hour refresh, VRAM <7.8GB steady, spot-reads) — nothing measures the new default yet.
- **Interaction with your own step3 fix:** the walkthrough talk-poll is now `NPC_DIALOGUE_TIMEOUT_S + 8` = 20s — fine for a *warm* 9B, but a **cold first load** on the 3070 can exceed it (the provider's own timeout is 60s precisely for first-call load). On an Ollama-running box, the first chain of a walkthrough run may re-flake. Cheapest durable fix: pre-warm in harness setup when Ollama is detected (one throwaway generate), or an env-gated mock-AI mode for smokes.

## 4 · 🟡 New finding: the hygiene-batch's own red — a *cross-platform alias* collision one layer below the key dedup

`test_hygiene_batch_2026_07_03.py::TestHelpDupKeysDeduped::test_alias_deepscan_still_resolves_to_space_topic` **fails on Linux** (`deepscan` → `+sensors`, expected `space_anomalies`): the key dedup landed, but the **alias** `deepscan` is still claimed by both `data/help/topics/anomalies.md` (key `space_anomalies`) and `data/help/commands/+sensors.md` (`aliases: [sensors, sensor, scan, deepscan]`), and `os.walk` order decides the winner — your box's ordering vs mine flips it. Fix-shape: (a) give `deepscan` one deliberate owner — it's the typed anomaly-resolution verb per `salvage.md`/`navigation.md` prose, so the anomalies topic; drop it from `+sensors.md`'s aliases and keep the cross-ref in `see_also` (also decide `scan`, claimed there too, against whatever the ground `scan` verb's help should be); (b) make the loader deterministic (sorted walk) **and** extend the dup-key detection to **aliases** with the same warn/ratchet, so the next one is loud on every OS. The batch's test then goes green cross-platform without weakening the pin.

## 5 · Design notes (small, for the record)

- **Phantom Tonnage:** the venue-shape resolution is exactly right, and the honesty about the missing ship-combat seam is the best line in the entry. Two records to keep straight: it **consciously relaxes the lane's skill-uniqueness invariant** (`sensors`, `computer programming` reuse) — write that relaxation into the lane's invariant note explicitly so arc 37+, if it ever exists, can't cite it as silent precedent; and the 4-room liner is a reusable substrate — log it as venue stock for the staged-questline-archetype design pass already in OBS.
- **Suite-gate bookkeeping:** the gate now reads "16,285 passed, 8 failed — 5 accepted baseline + 3 SPA/jsdom parallel-contention flakes green in isolation." Promote that trio out of the arithmetic: serial-mark (or retry-once) the jsdom-subprocess tests so the accepted-red list stays exactly the accepted-red list.
- **Queue state is healthy:** 7 pending design calls, F4-reconfirmed correctly at the top for Brian; incorporation preserved my docs under `docs/fable/`. No drift observed in any standing invariant on this tree (AST/YAML clean; F1/anomaly/staged-cult seams all consistent with their entries).

**Suggested order:** §3 Qwen curl + idle_queue parse hardening (today — the default is live), §4 alias-collision fix + loader determinism (it's your own red), then provider extension/ctx/sampling as the spec's Drop-A/B cadence, then the §2 signal-producer audit pass before launch.

— Fable, 2026-07-03. Tree: `SW_MUSH_upload_20260703_0828.zip`.
