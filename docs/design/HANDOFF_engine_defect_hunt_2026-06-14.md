# HANDOFF — Engine defect-hunt findings (cold subsystems) — 2026-06-14

> Produced by the parallel defect-hunt session (worktree `c:\SW_MUSH`, branch
> `drop/t3-20-safe-load`). A find->adversarial-verify Workflow swept FIVE cold,
> non-contested engine subsystems (`harvest`, `housing`, `contest`, `dens`,
> `hazards`) for RUNTIME defects the static curated-list tests miss. Every item
> below was confirmed by an adversarial verifier that read the code and cited exact
> lines; **spot-check against HEAD before acting** (lines drift).

**Status:** these are CONFIRMED-but-UNFIXED at handoff. The session stood down
(token budget, yielding to the main thread) before implementing any fix — so this
is a clean, actionable backlog. All are assessed **parallel-safe** (fix contained
to the one engine file + a new test; none touch parser/director/achievements/
scenes/organizations/missions/chains/questline). Each is a faithful candidate for a
`drop-implementer` drop or a future defect-hunt session.

**Yield:** 16 confirmed real defect(s) across 5 cold engine subsystems.

Suggested grouping into drops (by file + theme):
- **housing.py** — `shopfront_sell` room/exit leak (wrong SQL columns) + `checkout_room` internal-exit leak (both fixed by routing through `db.delete_room`); `is_on_guest_list` always-False membership; `purchase_home` charge-before-persist with no refund.
- **harvest.py** — false-success on credit-write AND resource-grant failure (cooldown already committed); payout reported pre-throttle.
- **contest.py** — anchor NPC + reinforcements never despawned on resolution (leak), plus the stale-anchor reuse / spawn-idempotency / resolution compare-and-swap cluster.
- **hazards.py** — severity scaled into difficulty TWICE; pickpocket credit desync (pre-mutates `char['credits']`, discards `adjust_credits` return — inverts the canonical funnel pattern).
- **dens.py** — establish refund not rollback-safe; read-swallow turns DB error into 'no den'; abandon zero-row false success.

---

## HIGH

### `housing` :: shopfront_sell_wrong_exit_columns_room_leak

- **File / lines:** `c:/SW_MUSH/engine/housing.py` 2599-2606
- **Class:** silent-swallow / false-success  |  **Confidence:** 0.9  |  **Effort:** small  |  **Parallel-safe:** True
- **Defect:** sell_shopfront() deletes shopfront rooms with `DELETE FROM exits WHERE from_room = ? OR to_room = ?`, but the exits table columns are `from_room_id` / `to_room_id` (db/database.py:91-92). SQLite raises OperationalError: 'no such column: from_room'. The exit-delete and the room-delete are in the SAME try block, so the room delete on the next line never runs, and the exception is swallowed by the bare `except` that just logs. The function still pays the 50% refund (line 2587), decrements lot occupancy (2611), and deletes the player_housing record (2620), then returns {ok: True}.
- **Failure path:** Concrete sequence: a player buys a shopfront (purchase_shopfront, tier=4) then runs `housing sell` -> sell_shopfront(). For every room id, line 2601 hits the bad column name and raises immediately; line 2603's `DELETE FROM rooms` never executes; the except at 2604 swallows it. Net: every shopfront room (1-5 rooms) and all internal exits LEAK permanently. The leaked rooms keep `housing_id` pointing at a player_housing row that line 2620 just deleted -> dangling FK / orphan rooms reachable via their still-live internal exits. The caller is told the sale succeeded. This is the classic false-success + room-leak combo the prompt flags; the curated tests never assert room deletion for sell_shopfront so it passes green.
- **Verifier confirmation:** CONFIRMED. exits schema (db/database.py:91-92) is `from_room_id INTEGER NOT NULL`, `to_room_id INTEGER NOT NULL` — there is no `from_room`/`to_room` column (grep of database.py shows `from_room` only as a Python param name in create_exit/delete_exit_by_dir/find_exit_by_dir, never a SQL column). db.execute (database.py:1609-1615) is a thin pass-through to aiosqlite.execute, so `await db.execute("DELETE FROM exits WHERE from_room = ? OR to_room = ?", ...)` at housing.py:2601-2602 raises OperationalError: no such column: from_room. That raise happens INSIDE the same try block as the room delete (line 2603 `DELETE FROM rooms WHERE id = ?`), so the room delete never runs; the bare except at 2604-2606 only logs. Meanwhile the refund (2587), lot-occupancy decrement (2611), player_housing delete (2620), home_room_id clear (2623), and commit (2633) all proceed, and the function returns {ok: True} (2638). Net: every shopfront room AND its internal exits leak while the caller is told the sale succeeded. No existing test exercises sell_shopfront (grep of tests/ for sell_shopfront returns nothing), so it passes green.
- **Recommended fix:** Replace housing.py:2601-2603 with `await db.delete_room(rid)` (db/database.py:2040-2052, which deletes both directions of exits via the correct from_room_id/to_room_id columns then deletes the room), wrapping only that call in the try/except. Add a regression test that purchases a shopfront then asserts every room id and internal exit is gone after sell_shopfront. Fix is entirely within engine/housing.py + new test.

### `contest` :: anchor_npcs_never_despawned

- **File / lines:** `c:/SW_MUSH/engine/contest.py` 1521-1644 (_resolve_challenger_win), 824-895 (_resolve_defender_win), 1429-1506 (on_npc_killed_in_combat tick path)
- **Class:** phantom producer/consumer  |  **Confidence:** 0.88  |  **Effort:** medium  |  **Parallel-safe:** True
- **Defect:** When a region contest resolves by ANY path, the Region Anchor NPC and all reinforcement NPCs spawned by _spawn_region_anchor are never deleted. _resolve_challenger_win calls dismiss_region_garrison(db, region_slug) (line 1565) and _resolve_defender_win calls nothing; but the Anchor + reinforcements are created via db.create_npc (lines 1329, 1368) and are NEVER registered in the region_garrison table, so dismiss_region_garrison (which only DELETEs npcs whose ids are in region_garrison, territory.py:1710-1728) cannot reach them. No reaper tick exists either. The contest row records anchor_npc_id but no resolution path deletes that npc. The docstring at line 1542 even claims 'Mark resolved + dismiss the Anchor + reinforcements' but the code only dismisses the garrison.
- **Failure path:** The overwhelmingly common outcome is defender-win-by-default at ends_at (challenger fails to break the Anchor in the 4h window): tick_region_contest_resolution Phase B -> _resolve_defender_win marks status='resolved_defender' and returns WITHOUT touching the anchor NPC. The Anchor (a hostile, tier-2, HP-buffed NPC) plus 0-4 hostile reinforcements remain alive and aggressive in the landmark room forever. Players entering that room after the contest ends are attacked by a contest NPC that no longer represents any active contest. On the challenger-win path the same leak occurs (dismiss_region_garrison touches the loser's garrison, not the Anchor). Every contest that runs to the culminating fight orphans 1-5 permanent hostile NPCs.
- **Verifier confirmation:** CONFIRMED. _resolve_defender_win (lines 824-895) only does: UPDATE status='resolved_defender' (841-846), adjust_territory_influence penalty (854-863), _set_contest_cooldown (866), broadcast (881-894). It never references the anchor NPC, never calls delete_npc, never calls dismiss_region_garrison. _resolve_challenger_win calls dismiss_region_garrison(db, region_slug) (line 1565), but that helper (territory.py:1700-1744) deletes only NPCs whose ids are rows in region_garrison (SELECT npc_id FROM region_garrison WHERE region_slug=?, then DELETE FROM npcs). Grep of contest.py shows ZERO 'INSERT INTO region_garrison' — the anchor is created with db.create_npc (line 1329) and is never registered in region_garrison, so dismiss_region_garrison cannot reach it. Reinforcements are tagged ai_config_json['anchor_reinforcement_for']=contest_id (line 1367) but no code path deletes by that tag. The schema (lines 204-217) has no FK/trigger cascading to npcs. Grep across all of engine/ (excluding contest.py) shows no reaper/despawn for the contest anchor — every delete_npc/despawn hit belongs to wilderness/hunters/guards/housing, none to contests. The docstring line 1542 'Mark resolved + dismiss the Anchor + reinforcements' is unfulfilled. Defender-win-by-default (Phase B, the common outcome) leaks the anchor + 0-4 reinforcements as permanent hostile NPCs; challenger-win leaks the same set since dismiss_region_garrison only touches the loser's garrison.
- **Recommended fix:** Add a shared async helper in engine/contest.py (e.g. _despawn_contest_anchor(db, contest)) that, after status is flipped, reads contest['anchor_npc_id'] and calls db.delete_npc(anchor_id), then deletes reinforcements via DELETE FROM npcs WHERE json_extract(ai_config_json,'$.anchor_reinforcement_for') = <contest_id> (commit, each guarded by try/except WARNING). Call it from _resolve_defender_win, _resolve_challenger_win, and the on_npc_killed_in_combat dead-anchor case. db.delete_npc is an existing db method (database.py:3293), so no contested file is touched.

### `hazards` :: severity_difficulty_double_scaled

- **File / lines:** `engine/hazards.py` 336-338, 584, 631-635
- **Class:** D6 math  |  **Confidence:** 0.88  |  **Effort:** small  |  **Parallel-safe:** True
- **Defect:** Hazard difficulty is scaled by severity TWICE for every hazard whose stored config carries a pre-computed `difficulty`. Both producers of a hazard dict bake the severity scaling INTO the stored difficulty: the wilderness synthesizer (L584: `"difficulty": template["base_difficulty"] + (severity - 1) * 3`) and `set_room_hazard` (L634: same expression). Then `check_hazard_for_character` reads that already-scaled value (L336 `difficulty = hazard_cfg.get("difficulty", template["base_difficulty"])`) and at L338 applies `difficulty += (severity - 1) * 3` a SECOND time.
- **Failure path:** Concrete live trigger: `data/worlds/clone_wars/dune_sea.yaml` and `ey_akh.yaml` tag tiles with `ambient_hazard: extreme_heat`, `hazard_severity: 2`. The wilderness path builds the synthetic room with difficulty `base(10) + (2-1)*3 = 13` at L584, then L338 adds `(2-1)*3 = 3` AGAIN → 16, then the day-band tod mod (+3) → 19. The DESIGNED difficulty is `10 + 3(severity) + 3(day) = 16`. Players take a stamina check vs 19 instead of 16 — a full extra +1D harder than authored. The error grows with severity: a radiation tile at severity 5 stores `15 + 12 = 27`, doubles to `15 + 24 = 39` — a near-unbeatable stamina target. Identical bug on the room path: any hazard placed via `set_room_hazard` stores a scaled difficulty and is double-scaled at tick time. The curated/static tests miss it because every example that round-trips at severity 1 has `(1-1)*3 = 0`, so the double-add is a no-op precisely at the value the tests exercise.
- **Verifier confirmation:** CONFIRMED. Both producers bake severity scaling into the stored difficulty: wilderness synthesizer L584 `"difficulty": template["base_difficulty"] + (severity - 1) * 3` and set_room_hazard L634 same expression. check_hazard_for_character then reads that pre-scaled value at L336 `difficulty = hazard_cfg.get("difficulty", template["base_difficulty"])` and re-applies the SAME scaling at L338 `difficulty += (severity - 1) * 3`. For a severity-2 extreme_heat tile: stored 10+(2-1)*3=13, then L338 -> 13+3=16, then the day-band TOD mod at L355 (+3) -> 19, versus the authored 10+3(sev)+3(day)=16. Live triggers verified: data/worlds/clone_wars/wilderness/dune_sea.yaml L101-102 (ambient_hazard: extreme_heat, hazard_severity: 2) and ey_akh.yaml L58-59 (same), plus hazard_severity_bonus:1 variant overlays (dune_sea L260, coruscant_underworld L310) that push it higher. Tests miss it because the only severity>1 test, tests/test_t2_wenc_wilderness_encounters.py:678, stub-patches check_hazard_for_character (L704-709) so the L338 scaling never runs, and asserts only type/severity in the stored props (L738-739), never the final difficulty; test_lane_e2b_clock.py only unit-tests _extreme_heat_time_mod in isolation. At severity 1 the (1-1)*3=0 double-add is a no-op, masking it in all curated examples.
- **Recommended fix:** Make L338 the single scaling site: stop pre-scaling in the producers — store raw `"difficulty": template["base_difficulty"]` at L584 (wilderness synthetic room) and L631-635 (set_room_hazard). Add a regression test that captures perform_skill_check's difficulty arg for a severity-2 extreme_heat tile with TOD forced to 'dusk' (mod 0) and asserts 13, not 16. NOTE the candidate's parallel_safe_guess=false is REFUTED: the stored difficulty field is read back ONLY by engine/hazards.py:336 (grep over the tree confirms no other code consumer); parser/director_commands.py consumes only set_room_hazard's {ok,msg} return, and engine/espionage.py reads only hazard type/severity (L295-298), never difficulty. No test asserts on the stored difficulty number. Fix is fully contained to engine/hazards.py + a new test.

### `harvest` :: false_success_credit_write_failure

- **File / lines:** `engine/harvest.py` 846-852, 879-887, 924-940
- **Class:** silent-swallow / false-success  |  **Confidence:** 0.85  |  **Effort:** small  |  **Parallel-safe:** True
- **Defect:** On a successful harvest, the 30-min cooldown is set and persisted at Step 7 (lines 846-852) BEFORE credits are granted at Step 9 (lines 879-881). If db.adjust_credits raises, the broad except at lines 882-884 swallows it and execution falls through to the Step 11 success return (lines 924-940) with ok=True and credits_kept=payout['credits_kept'] (a positive number) plus a '+NNNcr' success message — even though NO credits were actually written. The player is told they harvested N credits, sees nothing in their balance, and is locked out of that region for 30 minutes (cooldown already committed). This mirrors the breaching delete-failsafe bug: success reported despite a real write failure.
- **Failure path:** Concretely reproduced: with a DB whose adjust_credits raises RuntimeError, perform_harvest returned ok=True, msg='Harvested tat_dune: +488cr + 2 metal (q100), 1 chemical (q100)', credits_kept=488, while char['credits'] stayed 0. The cooldown was already set+saved at Step 7, so the player cannot retry. Trigger: any transient aiosqlite/DB error (lock timeout, disk I/O, connection drop) during the credit write — exactly the class of failure adjust_credits' own docstring warns about.
- **Verifier confirmation:** CONFIRMED against code. Step 7 sets+persists the cooldown FIRST (line 846 set_cooldown(char, cd_key, HARVEST_COOLDOWN_SECS), lines 847-852 save_character with the broad except logging-only). Step 9 then routes credits at lines 879-881 'char["credits"] = await db.adjust_credits(char["id"], payout["credits_kept"], "harvest")' wrapped in try/except 882-887 that ONLY logs ('credit save failed ... we proceed but log loud') and does NOT mutate the result or set a failure flag. Execution then falls straight through to Step 11 (lines 924-940) which returns ok=True, credits_kept=payout['credits_kept'] (a positive number unaffected by the failed write), and msg=_format_success_msg(...) which renders '+{payout["credits_kept"]:,}cr' (line 948). So on any adjust_credits raise (DB lock / I/O / connection drop), the player is told they harvested N credits, char['credits'] is never updated to a credited value, and the 30-min region cooldown is already committed (Step 7) so they cannot retry. This is the same false-success-despite-write-failure class flagged. Note the comment at 822 deliberately does NOT set cooldown when the skill-check engine errors, proving the codebase already knows engine faults shouldn't farm the cooldown — the credit-write path violates that posture.
- **Recommended fix:** Track whether the credit grant committed. Move the cooldown set+save (Step 7) to AFTER the credit grant has committed, OR in the Step 9 except set a flag and downgrade the Step 11 return to ok=False with a 'survey instruments hiccup, try again' message while skipping/clearing the cooldown — mirroring the Step-5 skill-check-error posture (lines 819-826) that deliberately does not consume the cooldown on an engine fault. Add a test with a MiniDB whose adjust_credits raises asserting (ok=False) OR (credits_kept==0 and char['credits'] unchanged) so the success line can never overstate the payout. Contained to engine/harvest.py + a new test.

### `housing` :: is_on_guest_list_int_in_dict_list

- **File / lines:** `c:/SW_MUSH/engine/housing.py` 2866-2869
- **Class:** phantom producer/consumer  |  **Confidence:** 0.85  |  **Effort:** trivial  |  **Parallel-safe:** True
- **Defect:** is_on_guest_list(h, char_id) does `guests = _guest_list(h); return char_id in guests`. But the guest list is stored as a list of dicts {"id": int, "name": str} (written by guest_add at line 2096). `int in [dict, dict, ...]` is always False, so an explicitly-added guest is never recognized.
- **Failure path:** Concrete sequence: owner runs `housing guest add Bob` -> guest_add stores {"id": <bob_id>, "name": "Bob"} in guest_list (line 2096). Bob then walks toward the owner's private room. MoveCommand (parser/builtin_commands.py:1153) calls can_enter_housing_room(), which at line 2890 calls is_on_guest_list(h, bob_id). `bob_id in [{"id": bob_id, "name": "Bob"}]` evaluates False, so the function falls through to `return False, "The door is locked."` (line 2893). The guest feature is a written-but-never-readable producer/consumer mismatch: guests can be added and listed (get_guest_list_display reads g.get('name')) but the permission gate keys off the wrong shape, so NO guest can ever actually enter. The static suite has no test exercising can_enter_housing_room/is_on_guest_list for housing.
- **Verifier confirmation:** CONFIRMED. guest_add (housing.py:2096) appends `{"id": guest["id"], "name": guest["name"]}` to the guest_list, and the dedup check at 2092-2093 reads `g.get("id")` while get_guest_list_display (2138-2139) reads `g.get('name')` — so the stored shape is unambiguously a list of dicts. `_guest_list(h)` (1802-1803) just json-loads that list with no normalization to ids. is_on_guest_list (2866-2869) does `char_id in guests`, i.e. an int membership test against a list of dicts, which is always False. The consumer can_enter_housing_room (2890) calls is_on_guest_list and, on False, returns (False, "The door is locked.") at 2893; that gate is wired into the real move path (parser/builtin_commands.py:1153 blocks movement when not allowed). So an explicitly added guest can never enter. No test covers is_on_guest_list/can_enter_housing_room (grep returns nothing).
- **Recommended fix:** Change housing.py:2869 to inspect dict ids: `return any((g.get("id") if isinstance(g, dict) else g) == char_id for g in guests)`. Add a unit test: guest_add a player, then assert can_enter_housing_room returns (True, "") for that guest id. The consumer (can_enter_housing_room, builtin_commands move path) is unchanged; fix is contained to engine/housing.py + new test.

### `dens` :: establish_commit_fail_refund_leaks_free_den

- **File / lines:** `engine/dens.py` 168-184
- **Class:** state-machine corruption  |  **Confidence:** 0.62  |  **Effort:** small  |  **Parallel-safe:** True
- **Defect:** establish_den's write-failure refund path is not transaction-safe against a commit() failure that occurs AFTER the den INSERT already executed. The den INSERT (line 169) and the den commit (line 174) run on the shared aiosqlite connection (db._db), which uses deferred-transaction isolation (no isolation_level set, no rollback() anywhere in db/database.py). If line 169's INSERT OR REPLACE succeeds but line 174's commit() raises (transient SQLITE_BUSY / disk-full / lock), the code logs 'write failed; refunding' and calls db.adjust_credits(cost, refund) (lines 179-180). adjust_credits internally runs self._db.execute(UPDATE) + self._db.commit() (database.py 2764-2768) on that SAME connection — and that commit flushes the entire still-pending transaction, INCLUDING the orphaned sabacc_dens INSERT. Net result: the establisher is fully refunded the 10,000-credit setup cost AND the den row becomes durably committed. The org now operates a working sabacc den (rake begins routing to its treasury) that no one paid the setup sink for.
- **Failure path:** Concrete trigger: a SQLITE_BUSY at the den-commit (the codebase explicitly notes the 1 Hz tick loop contends with interactive writes — busy_timeout=5000 was added for exactly this, database.py 1522-1524). aiosqlite/sqlite3 defaults to deferred isolation; the INSERT at 169 auto-opens a txn that is NOT rolled back when commit() at 174 raises (there is zero rollback() in db/database.py). The very next commit on the connection — issued by the refund's adjust_credits — persists the pending INSERT. So the failed-write branch's intended 'undo' (refund) actually CO-COMMITS the thing it was trying to undo, while also returning the player's money. The setup-cost sink (the whole economic point of dens, per the module docstring) is bypassed and the org gains a free faucet of org-treasury income.
- **Verifier confirmation:** CONFIRMED. dens.py:169-174 runs `await db._db.execute(INSERT OR REPLACE INTO sabacc_dens ...)` then `await db._db.commit()` on the shared aiosqlite connection. db/database.py opens that connection via `aiosqlite.connect(self.db_path)` (line 1518) with NO isolation_level set -> Python sqlite3 default deferred autotxn; grep for `rollback|isolation_level` over db/database.py finds ZERO rollback calls (the one hit at 4802 is a comment about the `give` command). So if commit() at 174 raises (SQLITE_BUSY is a live risk: busy_timeout=5000 PRAGMA exists at database.py:1524 precisely because the 1 Hz tick loop contends with interactive writes), the INSERT stays pending. The except at 175-184 then calls db.adjust_credits(char_id, +cost, 'sabacc_den_setup_refund'). Critically the refund source contains 'refund', so the throttle no-op short-circuit at database.py:2747-2748 is SKIPPED, guaranteeing the unconditional `self._db.execute(UPDATE)` + `self._db.commit()` at database.py:2763-2768 fires on the SAME connection -> that commit flushes the still-pending orphaned INSERT. I reproduced the SQLite semantic directly: with deferred isolation, an INSERT left pending after a (would-be failed) commit is co-committed by the next commit on the connection (den row count -> 1, char balance -> fully refunded). Net: durable free den + full 10k refund, bypassing the setup-cost sink. The existing test_establish_write_failure_refunds (tests/test_a5_dens.py:176-185) masks this because _FakeDB.execute raises BEFORE recording (line 130-131) so no row is ever pending and commit() is a no-op.
- **Recommended fix:** In the except at dens.py:175-184, before issuing the refund adjust_credits, best-effort discard the pending INSERT: `try: await db._db.rollback() except Exception: pass`. That drops the orphaned den row so the refund's commit cannot resurrect it. Cover with a test using a real aiosqlite :memory: connection (or a fake that records the INSERT, raises on the first commit, then succeeds) asserting both net-zero credits AND COUNT(*) FROM sabacc_dens == 0.

## MEDIUM

### `hazards` :: theft_credit_dict_desync_inverted_order

- **File / lines:** `engine/hazards.py` 428-434
- **Class:** silent-swallow / false-success  |  **Confidence:** 0.78  |  **Effort:** trivial  |  **Parallel-safe:** True
- **Defect:** The urban_danger pickpocket mutates the in-memory session credits BEFORE the ledger write and then DISCARDS the authoritative return value of `adjust_credits`, inverting the canonical `char["credits"] = await db.adjust_credits(...)` pattern used everywhere else (commissary.py, housing.py, harvest.py, dens.py, etc.). L429 sets `char["credits"] = char.get("credits", 0) - stolen` (a local guess), then L432 calls `adjust_credits(char_id, -stolen, "hazard_theft")` inside a try/except that swallows any failure (L433-434).
- **Failure path:** `adjust_credits` mutates the DB row via an atomic SQL increment and does NOT touch the in-memory dict; the contract enforced by every other call site is that the dict mirrors the RETURNED balance. Here the return is thrown away. Failure path: if `adjust_credits` raises (DB locked / transient error), it is silently swallowed at L433, but `char["credits"]` was ALREADY decremented at L429 and the player is told `"You lost N credits to a pickpocket!"` at L442 — a false-success: the dict and the player message assert a theft the ledger never recorded. `char = s.character` is the LIVE session dict that feeds the web client balance, so the displayed balance is now lower than the true DB value until reload, when the credits 'reappear'. Also, because the dict is set from a local computation rather than the post-write balance, any throttle/clamp logic inside `adjust_credits` (it scales faucets and can refuse sinks) would diverge silently.
- **Verifier confirmation:** CONFIRMED. L429 pre-decrements the LIVE session dict (`char["credits"] = char.get("credits", 0) - stolen`) BEFORE the ledger write, then L432 calls `await db.adjust_credits(char_id, -stolen, "hazard_theft")` inside a try whose except (L433-434) swallows any failure, and DISCARDS the return. This inverts the canonical pattern confirmed at ~30 call sites (commissary.py, housing.py, harvest.py, dens.py, chain_rewards.py, etc.) which all do `char["credits"] = await db.adjust_credits(...)`. db/database.py:2671 confirms adjust_credits returns the new balance via an atomic SQL increment and does NOT mutate the in-memory dict, so the dict is meant to mirror the RETURNED value. Concrete failure path: if adjust_credits raises (the function does atomic DB writes that can hit a locked/transient error), L429 already decremented the live dict, L433 swallows the error so the ledger recorded nothing, and the 'You lost N credits to a pickpocket!' message at L442 — which sits in an `if session:` block at the SAME indent as `if db:` (L435 vs L430), OUTSIDE the try — fires unconditionally. Result: a false-success — the web client balance (s.character is the live dict) drops below the true DB value until relog, when the 'stolen' credits reappear. Severity is medium: on the happy path the local `current - stolen` coincidentally equals the DB result (the L428 guard pre-checks affordability, allow_negative defaults True so no refusal, and the faucet throttle only touches delta>0 not this sink), so the desync only manifests on the DB-error path.
- **Recommended fix:** Mirror the canonical pattern inside the urban_danger branch: drop the L429 pre-mutation; inside `if db:` assign the return — `new_bal = await db.adjust_credits(char_id, -stolen, "hazard_theft"); char["credits"] = new_bal`; and move the 'You lost N credits' pose_event so it only fires after a successful, non-None write (so a swallowed DB failure reports no phantom theft). Keep a no-db fallback for tests. Entirely contained to engine/hazards.py + a test; touches none of parser/director/achievements/scenes/organizations/missions/chains/questline.

### `harvest` :: false_success_resource_grant_failure

- **File / lines:** `engine/harvest.py` 909-921, 924-940
- **Class:** silent-swallow / false-success  |  **Confidence:** 0.7  |  **Effort:** small  |  **Parallel-safe:** True
- **Defect:** Same false-success pattern at the resource-grant site (Step 10). add_resource mutates char['inventory'] in memory, then save_character(inventory=...) persists it (line 918). The broad except at lines 919-921 swallows any failure (a save_character DB error, or a json/shape error in add_resource) and execution falls through to the success return reporting payout['resource_stacks'] as granted. Additionally, add_resource RETURNS an error string (not raising) when rtype not in RESOURCE_TYPES (engine/crafting.py:449-450); the loop at lines 915-917 ignores the return value, so an unknown resource type silently vanishes while still being listed in the '+ 2 metal, 1 chemical' success message.
- **Failure path:** If save_character(inventory=...) raises on the resource write (DB lock, I/O), the harvester sees 'Harvested: +2 metal (q50)...' but the resources never persist to the DB — the next session reload shows nothing. The in-memory char['inventory'] was mutated, so within the same session it looks granted, masking the loss until reload. Cooldown is already consumed (Step 7), so no retry. The unknown-type silent-drop is latent today (all current yield/bonus types are in RESOURCE_TYPES) but becomes live the moment a future yield row or _REGION_SCAVENGE_BONUS target is added that isn't registered.
- **Verifier confirmation:** CONFIRMED against code, with the severity caveat the author noted. Step 10 (lines 912-921): for each stack add_resource(char, ...) mutates char['inventory'] in memory (engine/crafting.py:430 char['inventory']=json.dumps(inv) inside _set_resource_list), then line 918 'await db.save_character(char["id"], inventory=char["inventory"])' persists it. The except at 919-921 only logs ('Same posture as credits — log and continue') and does NOT alter the result, so Step 11 (924-940) returns ok=True with resource_stacks=payout['resource_stacks'] and the success msg lists them ('+ 2 metal (q50)...' via _format_success_msg 949-958) even when the save raised — resources never persisted, next reload shows nothing, cooldown already consumed (Step 7), no retry. Secondary claim also CONFIRMED but LATENT: add_resource RETURNS 'Unknown resource type: {rtype}' (engine/crafting.py:449-450) rather than raising; the loop at 915-917 discards the return value, so an unregistered type would silently vanish while still being listed in the success message. I verified every type the code can currently emit IS registered: YIELD_TABLE types (metal/chemical/organic/rare) and _T5_RARE_TARGET='rare' are in RESOURCE_TYPES (crafting.py:42), and _REGION_SCAVENGE_BONUS target 'scavenged_republic_tech' is in RESOURCE_TYPES (crafting.py:59). So the silent-drop is dormant today and only goes live if a future yield/bonus row names an unregistered type. The persistence-failure false-success, however, is live now. Medium severity is defensible: lower blast radius than the credit bug since the in-memory char masks it within the session and resources are the secondary income lever.
- **Recommended fix:** On the Step-10 except, rebuild resource_stacks in the result from what actually landed in char['inventory'] (or downgrade to ok-with-warning) so the success line never lists unpersisted resources; persist resources before consuming the cooldown for full fairness. Separately, capture add_resource's return value and drop/log any 'Unknown resource type' rejections from the reported result so the message can't list a silently-dropped stack. Contained to engine/harvest.py + a new test (MiniDB whose save_character raises when 'inventory' is in kwargs; assert reported vs persisted gap is closed).

### `contest` :: stale_anchor_reuse_create_npc_dedup

- **File / lines:** `c:/SW_MUSH/engine/contest.py` 1324, 1329-1340, 1342-1353
- **Class:** state-machine corruption  |  **Confidence:** 0.7  |  **Effort:** trivial  |  **Parallel-safe:** True
- **Defect:** The Anchor name is deterministic: anchor_name = f"{tmpl['name_prefix']} of {region_slug}" (line 1324). db.create_npc has a duplicate-name-in-room guard (db/database.py:3247-3252) that returns the EXISTING npc id instead of creating a new one. Because the previous contest's Anchor is never despawned (see anchor_npcs_never_despawned), a later contest on the same region whose random landmark choice lands on the same room will get the stale prior Anchor's id back, and pin anchor_npc_id to a leftover NPC body rather than a fresh one.
- **Failure path:** Contest A on slug ends defender-win-by-default; its Anchor 'Republic Sector Commander of <slug>' survives at landmark L (leaked). After the 14-day cooldown, contest B is declared on the same slug; Phase A picks landmark L (1-in-N chance, or guaranteed if the region has one landmark) and calls create_npc with the identical name+room. The dedup guard returns the OLD anchor's id. Contest B's anchor_npc_id now points at a possibly-mis-statted, possibly-already-engaged leftover NPC; killing it resolves contest B using a body that was never freshly HP-scaled for B's defender influence, and the [CULMINATING FIGHT] broadcast announces an Anchor that already physically existed.
- **Verifier confirmation:** CONFIRMED, contingent on defect 1. anchor_name = f"{tmpl['name_prefix']} of {region_slug}" (line 1324) carries no contest_id, so it is identical across successive contests on the same region. create_npc (database.py:3247-3252) returns the EXISTING npc id when a row with the same (name, room_id) already exists. _get_region_landmarks (territory.py:1530-1546) returns all region rooms; _r.choice(landmarks) (line 1296) may select the same landmark as a prior leaked anchor (guaranteed if the region has a single landmark). Because defect 1 leaves the prior anchor alive, a same-region same-landmark re-spawn calls create_npc with identical (name, room_id) and gets the stale id back; the freshly-built sheet from _build_anchor_sheet (line 1325) is silently discarded and anchor_npc_id (lines 1344-1349) pins the leftover body. Real failure path, lower frequency (needs the same landmark drawn), so medium severity is right.
- **Recommended fix:** Primary remediation is defect 1 (despawn on resolution). Belt-and-suspenders inside contest.py: make the anchor name unique per contest, e.g. f"{tmpl['name_prefix']} of {region_slug} (#{contest_id})" at line 1324, so create_npc's (name, room_id) dedup cannot collide with a leftover. Both are contest.py-only.

### `housing` :: purchase_home_charge_before_persist_no_refund

- **File / lines:** `c:/SW_MUSH/engine/housing.py` 1966-2011
- **Class:** silent-swallow / false-success  |  **Confidence:** 0.6  |  **Effort:** small  |  **Parallel-safe:** True
- **Defect:** purchase_home() debits the full cost via adjust_credits at line 1966, then performs the INSERT into player_housing, room housing_id updates, lot occupancy update and final commit (lines 1974-2011) with NO try/except and NO refund-on-failure. If the INSERT or commit raises, the credits are already gone and the player has paid for nothing. Contrast purchase_home_prestige (lines 756-783) which explicitly debits-then-refunds-on-persist-failure.
- **Failure path:** If the player_housing INSERT or the final db.commit() at line 2011 fails for any reason (db lock, constraint, transient aiosqlite error), the exception propagates out of purchase_home uncaught -> the caller sees an error, but adjust_credits at 1966 already committed the debit to credit_log/credits. The player loses 5,000-25,000cr with no home and no refund. The codebase's own prestige-purchase sibling demonstrates the correct refund-safe pattern, so this is an inconsistency the team already knows how to fix. Lower confidence than the column/guest bugs because it only triggers on a DB write failure (rare) rather than on normal input, but the money-loss blast radius is large.
- **Verifier confirmation:** CONFIRMED as a real (but rare-trigger) money-loss hole. purchase_home debits via adjust_credits at housing.py:1966, then runs the player_housing INSERT (1974-1986), room housing_id updates (1990-1993), lot occupancy update (1996-1999) and final db.commit() (2011) with NO surrounding try/except and no refund path — only the home_room_id UPDATE at 2002-2009 has a narrow swallow. If the INSERT or the commit raises (db lock, constraint, transient aiosqlite error), the exception propagates uncaught while the debit at 1966 is already applied, leaving the player out 5,000-25,000cr with no home and no refund. The sibling purchase_home_prestige (756-780) demonstrates the correct debit-then-refund-on-persist-failure pattern, so this is a known-fixable inconsistency. Real, but only on a DB write failure rather than normal input, hence medium severity / lower confidence than the column and guest bugs.
- **Recommended fix:** Wrap the post-charge persistence (INSERT + room/lot updates + commit, housing.py:1974-2011) in try/except; on exception refund via `await db.adjust_credits(char_id, cfg['cost'], 'housing_upgrade_refund')` and return {ok: False, reason: 'persist_failed'}, mirroring purchase_home_prestige (756-780). The core fix is contained to engine/housing.py + new test; the analogous hardening of purchase_shopfront/purchase_hq is optional and also stays within housing.py.

### `contest` :: anchor_spawn_not_idempotent_on_pin_failure

- **File / lines:** `c:/SW_MUSH/engine/contest.py` 1329-1340, 1342-1353, 787-806
- **Class:** state-machine corruption  |  **Confidence:** 0.6  |  **Effort:** small  |  **Parallel-safe:** True
- **Defect:** _spawn_region_anchor relies SOLELY on the Phase A SQL filter (anchor_npc_id IS NULL, line 792) for idempotency; it does not re-check contest['anchor_npc_id'] internally and does not roll back the just-created NPC if the pin fails. The anchor_npc_id UPDATE+commit (lines 1344-1350) is wrapped in a try/except that only logs a WARNING. If that commit fails, the NPC exists but anchor_npc_id stays NULL on the row, so the next hourly tick's Phase A re-selects the same contest and spawns AGAIN.
- **Failure path:** If the pin UPDATE or its commit raises (transient DB error, lock), the orphan Anchor is already live but unpinned. Next tick: Phase A re-runs, _r.choice(landmarks) may pick a different landmark, create_npc's dedup keys on (name, room) so a different room yields a SECOND distinct Anchor NPC. Now two hostile anchors exist; only the second can ever be killed-to-resolve (only its id is pinned). The first is an unkillable-for-resolution permanent hostile. Compounds with the leak finding.
- **Verifier confirmation:** CONFIRMED. Phase A idempotency rests solely on the SQL filter 'anchor_npc_id IS NULL' (line 792); _spawn_region_anchor never re-checks contest.get('anchor_npc_id') internally before creating. The pin UPDATE+commit (lines 1343-1350) is wrapped in try/except that only logs a WARNING (1351-1353) and does NOT return — execution falls through to reinforcement spawn, broadcast, and 'return int(anchor_npc_id)'. If that UPDATE or its commit raises, the anchor NPC is already live (created at 1329) but anchor_npc_id stays NULL on the contest row. The next hourly tick's Phase A re-selects the same contest (anchor_npc_id still NULL); _r.choice(landmarks) at 1296 may pick a different landmark, and create_npc's (name,room_id) dedup is defeated by the different room, yielding a SECOND distinct anchor — only the second is pinned/killable-to-resolve. Real, but gated on a transient failure of exactly that UPDATE/commit, so the lower confidence/severity is appropriate.
- **Recommended fix:** In contest.py: (a) at the top of _spawn_region_anchor, add 'if contest.get("anchor_npc_id"): return int(contest["anchor_npc_id"])' for defense in depth; (b) on pin failure, delete the just-created NPC (db.delete_npc(anchor_npc_id)) before returning so the next tick starts clean, or pin via 'UPDATE ... WHERE id=? AND anchor_npc_id IS NULL' and verify rowcount before broadcasting. All contest.py-only; db.delete_npc is not a contested file.

### `dens` :: db_read_swallow_false_no_den

- **File / lines:** `engine/dens.py` 87-110, 189-199
- **Class:** silent-swallow / false-success  |  **Confidence:** 0.55  |  **Effort:** small  |  **Parallel-safe:** True
- **Defect:** get_room_den (87-97) and get_org_dens (100-110) wrap the whole fetchall in `except Exception` and return None / [] respectively, silently converting a transient DB failure into 'no den exists'. abandon_den (197-199) consumes get_room_den: a swallowed read error makes abandon_den return {'ok': False, 'reason': 'no_den'} for a room that DOES have a den — a false negative the owner sees as 'there is no den here' instead of a retryable error. get_org_dens feeding any treasury/listing consumer would under-report owned dens the same way.
- **Failure path:** The except is unconditional (`except Exception`) and there is no re-raise — a SQLITE_BUSY / locked-table read (the same tick-loop contention the busy_timeout PRAGMA exists to fight) returns the empty/None sentinel that is indistinguishable from the legitimate 'no row' result. The caller has no way to tell 'no den' from 'DB hiccup', so it reports a wrong, terminal answer to the player and silently drops org-owned dens from any aggregate read.
- **Verifier confirmation:** CONFIRMED. get_room_den (dens.py:89-97) wraps the fetchall in unconditional `except Exception: ... return None`, and get_org_dens (102-110) returns `[]` the same way — no re-raise, so a transient SQLITE_BUSY/locked read returns the exact same sentinel as a legitimate 'no row'. abandon_den consumes it at 197-199: `den = await get_room_den(...); if not den: return {'ok': False, 'reason': 'no_den'}` -> a swallowed read error yields a terminal, misleading 'no_den' for a room that genuinely has a den, and the DELETE is never reached. There is a second, worse consumer: establish_den at line 146 (`existing = await get_room_den(db, room_id)`) feeds check_den_eligibility; a swallowed read returns existing=None, so the `already_den` guard (dens.py:74) is bypassed and the code proceeds to `INSERT OR REPLACE INTO sabacc_dens` (line 170), overwriting another org's existing den ownership. Both call sites convert a retryable DB hiccup into a wrong terminal answer.
- **Recommended fix:** Have the read helpers distinguish failure from absence: either narrow the catch and re-raise, or return a sentinel/raise a typed error so abandon_den can surface a distinct 'db_error' reason (not 'no_den') and establish_den can refuse rather than INSERT OR REPLACE over an unread existing den. All within engine/dens.py + a test stubbing fetchall to raise once.

## LOW

### `housing` :: checkout_room_internal_exit_leak

- **File / lines:** `c:/SW_MUSH/engine/housing.py` 972-986
- **Class:** async/aiosqlite misuse  |  **Confidence:** 0.8  |  **Effort:** trivial  |  **Parallel-safe:** True
- **Defect:** checkout_room() deletes only the entry/exit door pair via db.delete_exit(exit_id_in/out) (lines 974-977), then removes each room with a raw `DELETE FROM rooms WHERE id = ?` (line 984) that does NOT clean up exits. For multi-room Tier 3 private residences (standard=2 rooms, large=3 rooms), purchase_home creates internal exits room0<->room1<->room2 (lines 1958-1963) whose ids are never tracked or deleted. checkout/sell of such a home orphans those internal exits.
- **Failure path:** Sequence: player buys a 'large' Tier 3 home (3 rooms, internal exits created at purchase_home lines 1958-1963; only exit_id_in/out are persisted on the housing row). Player runs `housing sell` -> sell_home -> checkout_room. Lines 974-977 delete only the entry door pair; line 984 deletes the rooms via raw SQL that skips exits. The 4 internal exits (2 each direction between the 3 rooms) remain in the exits table pointing at now-deleted room ids. Pure leak (the rooms are gone so the exits are unreachable), no crash and no player-visible mis-behavior, hence low severity -- but it is genuine table cruft that grows with every multi-room checkout. faction_quarters and Tier-1 are single-room so unaffected; only Tier-3 multi-room hits this.
- **Verifier confirmation:** CONFIRMED. purchase_home creates internal exits between multi-room private residences at housing.py:1958-1963 (room0<->room1<->room2) and persists only exit_id_in/exit_id_out on the housing row (INSERT at 1974-1986) — the internal exit ids are never stored. checkout_room (940) sets `room_ids = _room_ids(h)` (all residence rooms), deletes only the door pair via db.delete_exit(exit_id_in/out) at 974-977, then loops `DELETE FROM rooms WHERE id = ?` at 984 which does NOT touch the exits table. So for standard (2-room) / large (3-room) residences the internal exits remain in the exits table pointing at deleted room ids. Pure leak: rooms are gone so exits are unreachable; no crash, no player-visible misbehavior. Single-room rented_room/faction_quarters are unaffected. Genuine but low-severity table cruft that grows per multi-room checkout/sell (sell_home at 2029 routes through checkout_room).
- **Recommended fix:** In checkout_room replace the `DELETE FROM rooms WHERE id = ?` loop (housing.py:982-986) with `await db.delete_room(rid)`, which removes both directions of every exit touching the room (database.py:2046-2050) then the room. Add a test asserting no exits remain referencing the residence rooms after checkout of a multi-room home. Contained to engine/housing.py + new test.

### `harvest` :: result_reports_unthrottled_payout_not_applied

- **File / lines:** `engine/harvest.py` 880-881, 890-896, 924-940
- **Class:** phantom producer/consumer  |  **Confidence:** 0.6  |  **Effort:** small  |  **Parallel-safe:** True
- **Defect:** The result dict and success message report payout['credits_kept'] and route payout['credits_tax'] to the owner treasury, but db.adjust_credits applies the global faucet throttle (db/database.py:2747-2752) and can scale the actual credited delta DOWN (e.g. 50% halves it). char['credits'] is set from the throttled return value, while res['credits_kept'] and the '+NNNcr' message still show the un-throttled number, and the owner still receives the full un-throttled 15% tax. So under any admin throttle < 100% the displayed/credited amounts diverge and credit conservation drifts (owner tax computed on gross the harvester never fully received).
- **Failure path:** adjust_credits('harvest') is a player faucet (delta>0, char_id>0, source has no p2p_transfer/refund token), so it IS throttled. With throttle=50, a 488cr harvest credits ~244 to the player but the message says +488cr and the owner treasury still gets round(gross*0.15). The mismatch is invisible at the default 100% but real the moment the @economy throttle is dialed down to fight inflation — precisely when accuracy matters.
- **Verifier confirmation:** CONFIRMED against code. db.adjust_credits applies the faucet throttle (db/database.py:2747-2752): for delta>0 with a source containing neither 'p2p_transfer' nor 'refund', delta = (delta*pct)//100. 'harvest' (lines 880-881) is exactly such a faucet (delta=payout['credits_kept']>0 on success, char_id>0, source 'harvest' has no exclusion token), so it IS throttled. Line 880 assigns char['credits'] to the adjust_credits return — which is the post-throttle new_balance (database.py:2774) — but Step 11 reports credits_kept=payout['credits_kept'] (the un-throttled number, line 928) and _format_success_msg prints '+{payout["credits_kept"]:,}cr' (harvest.py:948). Meanwhile the owner tax is computed in _compute_tax from credits_gross (un-throttled, harvest.py:331 tax=int(round(credits*NON_OWNER_TAX_RATE))) and routed in full at lines 890-896 via payout['credits_tax']. So with throttle=50 a 488cr harvest credits ~244 to the player but the result/message say +488cr and the owner still receives round(488*0.15). Real divergence + a conservation drift (owner tax taken on a gross the harvester never fully received). CORRECTLY rated low: at the default 100% throttle (database.py:2782-2789 default) this is an exact integer no-op and behaviourally invisible; it only manifests when an admin dials @economy throttle below 100%, and even then it is a display/accounting inaccuracy, not a lockout or lost grant.
- **Recommended fix:** Compute the harvester's actual applied delta from adjust_credits (capture old balance before the call, or use new_balance minus prior), and set res['credits_kept'] + the success message to that effective amount; optionally derive the owner tax from the same effective gross, or explicitly document that throttle does not touch tax. Minimal fix: report the actual applied delta as credits_kept rather than payout['credits_kept']. Contained to engine/harvest.py + a new test that sets faucet_throttle_pct=50 and asserts reported credits_kept matches the actual balance increase.

### `contest` :: resolution_update_missing_active_guard

- **File / lines:** `c:/SW_MUSH/engine/contest.py` 1544-1549 (_resolve_challenger_win), 841-846 (_resolve_defender_win)
- **Class:** state-machine corruption  |  **Confidence:** 0.55  |  **Effort:** small  |  **Parallel-safe:** True
- **Defect:** Both resolution functions UPDATE region_contests SET status=... WHERE id=? with no 'AND status=\'active\'' terminal-state guard, and the resolution is spread across multiple separate commits (status flip, then ownership transfer/penalty, then cooldown). on_npc_killed_in_combat re-queries status='active' before resolving (line 1458) so the SELECT is the only guard; there is no compare-and-swap on the write. Between the await db.fetchall guard read and the await db.execute status write, control can yield to the scheduler.
- **Failure path:** At the exact ends_at boundary, a player killing-blow on the Anchor (on_npc_killed_in_combat -> _resolve_challenger_win) and the hourly tick Phase B (ends_at <= now -> _resolve_defender_win) can both observe status='active'. Because the UPDATE has no WHERE status='active' clause, the second writer overwrites the first's terminal status (e.g. challenger-win ownership transfer already committed, then defender-win stomps status to 'resolved_defender' and slaps the now-winning challenger with a 25-influence penalty + 14-day cooldown). Narrow window, single-threaded async makes it rare, but the unguarded UPDATE is the root cause.
- **Verifier confirmation:** CONFIRMED but narrow. Both terminal-status writes use 'WHERE id = ?' with no 'AND status = ...' compare-and-swap: _resolve_defender_win line 841-845 (UPDATE ... SET status='resolved_defender' WHERE id=?) and _resolve_challenger_win line 1544-1548 (status='resolved_challenger' WHERE id=?), and neither checks affected rowcount. The only guard against double-resolution is the SELECT WHERE status='active' read in on_npc_killed_in_combat (1456-1458) and in the tick Phase B fetch (810-814); both read-then-write without a CAS, with await yield points between. At the ends_at boundary a player killing-blow (on_npc_killed_in_combat -> _resolve_challenger_win, which already commits ownership transfer) can interleave with the hourly tick (Phase B -> _resolve_defender_win), and the second writer overwrites status and double-applies a penalty/cooldown to the now-winning side. The UNIQUE(region_slug, status) constraint (line 216) does NOT prevent this since the two writers set different status values. Severity genuinely low: single-threaded async + requiring the killing blow to land inside the exact tick window makes it rare, but the unguarded UPDATE is the real root cause.
- **Recommended fix:** In contest.py add 'AND status = \'active\'' to both resolution UPDATE WHERE clauses and capture the cursor rowcount; only proceed with ownership transfer / penalty / cooldown / broadcast when the compare-and-swap actually flipped a row (rowcount == 1). Makes resolution idempotent and prevents double-apply. Contained to contest.py + a new test.

### `dens` :: abandon_delete_zero_rows_false_success

- **File / lines:** `engine/dens.py` 208-215
- **Class:** state-machine corruption  |  **Confidence:** 0.5  |  **Effort:** trivial  |  **Parallel-safe:** True
- **Defect:** abandon_den's DELETE (209-210) does not check rowcount. If a concurrent abandon (or a TOCTOU between the get_room_den at 197 and the DELETE) already removed the row, the DELETE affects 0 rows yet abandon_den still returns {'ok': True}. The command then reports 'you abandoned the den' to a second actor for a den that was already gone — a non-idempotent transition reported as a fresh success.
- **Failure path:** There is a real gap between the read (197) and the write (209) with no row-level guard or rowcount assertion; SQLite DELETE of a nonexistent row is a no-op that does not raise, so the except at 212 is never hit and ok:True is returned unconditionally. Two cartel members abandoning the same den in the same window both get a success message; downstream messaging/state assumes exactly one transition occurred.
- **Verifier confirmation:** CONFIRMED but minor. abandon_den (dens.py:208-215) issues `await db._db.execute('DELETE FROM sabacc_dens WHERE room_id = ?', ...)` without capturing the cursor or checking rowcount, then unconditionally `return {'ok': True}`. I verified the SQLite semantic: a DELETE of a row that another actor already removed (TOCTOU between get_room_den at 197 and the DELETE at 209) is a silent no-op with cursor.rowcount==0 and NO exception, so the except at 212 is never hit and ok:True returns. Two concurrent abandons of the same den both get the 'you abandoned the den' success. Real non-idempotency, but the consequence is purely cosmetic — a duplicate success message; no refund, no credit movement, no durable state corruption — hence low severity.
- **Recommended fix:** Capture the cursor: `cur = await db._db.execute('DELETE ...'); await db._db.commit(); if cur.rowcount == 0: return {'ok': False, 'reason': 'no_den'}`. aiosqlite cursors expose rowcount, so the fix is fully within engine/dens.py + a test asserting the second concurrent abandon returns ok:False/no_den.

---

## Method (repeat it)
Find->verify Workflow: one finder per (subsystem) reads the FULL engine file and
hunts 7 defect classes (funnel-bypass / silent-swallow / async-aiosqlite /
state-machine / boundary / D6-math / phantom); an adversarial verifier per
subsystem re-reads the code and default-REFUTES each finding, then rates
parallel-safety against the contested-file set. 5 finders -> 16 confirmed,
0 false-positives surfaced, 0 contested. The recurring root cause is the same
`false-success-despite-write-failure` / `wrong-DB-primitive-leaks-state` class the
breaching + encounter-funnel drops already fixed — a partial-coverage test blind
spot (curated tests never assert the failure branch).
