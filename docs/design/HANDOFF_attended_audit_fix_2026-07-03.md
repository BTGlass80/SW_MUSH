# HANDOFF — Attended session 2026-07-03 (morning → afternoon): recovery, staged-questline archetype, and the signal-producer audit

Attended main session, `c:\SW_MUSH`, HEAD `89772ce`. Companion to `docs/design/HANDOFF_overnight_2026-07-02_to_03.md` (the overnight lane that fed into this morning) and `docs/fable/FABLE_ADDENDUM_FOR_CLAUDE_CODE_2026-07-03.md` (source of the §2 signal-producer audit mandate). Authority order still holds — **CHANGELOG.md + TODO.json are ground truth**; this doc is the narrative index. Every claim below is grounded against `git log`/`git show` on the range `6be07ad..89772ce`; re-grep before acting.

## 1. Morning recovery — the capital-ship bridge was live-uncommitted work behind a phantom bookkeeping sweep

At session start, `6be07ad` had already landed CHANGELOG/TODO bookkeeping for `capital-ship-combat-bridge` while the actual code sat stalled, uncommitted, in the live tree — the "detached-gate implementer" failure pattern the overnight handoff flagged. Recovered via a 4-agent verify-drop fan-out (invariant-auditor, targeted tests, smoke, code-reviewer) before landing, per `2966527`'s commit message: **invariant-auditor CLEAN, targeted tests 783/0, smoke 227-boot CLEAN**, code-reviewer 4 findings → 3 fixed pre-landing + 1 accepted-as-convention:

- **MAJOR** — `handle_traffic_ship_destroyed` reentrancy: a set-once `TrafficShip.destroy_consumed` latch, checked before the first `await`; the handler now returns `Optional[int]` (`None` = kill already consumed) and `FireCommand._apply_hull_damage` bails on `None` before the wreck/achievement block. Kills the multi-gunner double-bounty/double-graduation race.
- `_engage_intercept` no longer claims "weapons hot" when `promote_to_combat` raises (honest running-dark variant, mirrors `_engage_combat`'s `spawned==0` gating).
- `_tick_ship` lifetime expiry now DEFERS while `in_live_combat` holds (a mid-fight ambient despawn could silently void the `space_combat_won` kill); `NpcSpaceCombatManager.remove_combatant` releases the hold (single ownership-release seam).
- Accepted-as-convention: step 1's `command_executed` also completes on `course intercept`'s scale-parity refusal path — matches every `command_executed` step in the corpus, flagged for the future command-syntax rework rather than special-cased.

+5 tests landed with the fix (17 total in `tests/test_space_combat_won_bridge.py`). Landed **`2966527`**.

## 2. The staged-questline archetype, first slice — "The Undertow Skim"

`staged-questline-archetype-slice1` (**`646e26a`**) shipped Brian's fork-stack ruling (1A·2A·3A·4B·5A·6B·7A·8B·9A, `DESIGN_staged_questline_archetype_2026-07-03.md`): a new `site_cleared` chain-step completion type bridging the T3.24 chain engine to the wilderness-anomaly substrate (`chain_missions.maybe_arm_site_for_step`, `chain_events.on_site_cleared`/`_match_site_cleared`, a `suppress_payout` flag on `WildernessAnomaly`/`spawn_scenario_anomaly` so a chain-armed scenario never double-pays). First-slice content: `kamino_undertow_skim` ("The Undertow Skim"), a 3-step questline anchored at a new Kamino venue (`data/worlds/clone_wars/wilderness/kamino_flooded_understructure.yaml`) running a standalone `kamino_undertow_purge` scenario template (wave → skill_gate → boss — the first content to exercise T3.23's `skill_gate` field). Pays a new ~900cr "staged" reward tier (relic + rep), deliberately above the flat 450cr freelance tier.

**The keep-both merge.** The staged-questline archetype (`site_cleared`) and the capital-ship bridge (`space_combat_won`) both touched the same completion-type seam (`engine/tutorial_chains.py::ALLOWED_COMPLETION_TYPES`, `engine/chain_events.py`, `data/worlds/clone_wars/tutorials/chains.yaml`, `data/achievements.yaml`, `tools/verify_tutorial_chains.py`, the T5 questline-content tests) — flagged in advance by the overnight handoff's MERGE WATCH. Resolved at **`19fc04e`** ("Merge origin/main (staged-questline-archetype-slice1): keep-both on the completion-type seam"): both frozenset entries, both hooks/matchers in `chain_events.py`, both arcs in `chains.yaml`, both achievements, both T5 walker branches survive. `chains.yaml` was rebuilt as base + both insertions after the textual merge interleaved the two appended chains. Merged-tree targeted battery: 149 passed.

## 3. Same-day landed by lanes/sessions

- **`bounty-track-skill-selection`** (**`80bed81`**, first commit of the day, 09:39) — `BountyTrackCommand.execute` (`parser/bounty_commands.py`) carried two swallowed `TypeError`s (`get_skill_pool(sk)` missing the registry arg; `DicePool.total_pips` called as a property, not a method) that pinned every hunter's investigation skill to the Search floor. Fixed with `get_cached_skill_registry()` + `pool.total_pips()`. New `tests/test_bounty_track_skill_selection_2026_07_03.py` (6). Surfaced `BOUNTY.track_third_skill_phantom` (the third ranked skill, `"tracking"`, is unregistered in `data/skills.yaml` and can never win) — still `OPEN` in `design_calls_pending_brian`.
- **`CHARGEN hardening`** (**`e00d07c`**) — 3 launch blockers + 1 minor from an onboarding break-it pass: 3/9 chargen templates (clone_trooper, republic_officer, cis_field_agent) summed to 53/54 pips (WEG-illegal); `force_sensitive` was directly settable via the web chargen SPA (removed the Force step from `chargen.html`, `chargen_validator` now rejects `force_sensitive=true`/`control`/`sense`/`alter` keys, both API handlers hard-code `force_sensitive=False`); `handle_submit` orphaned a 0-character account on a name collision (added a pre-check + TOCTOU backstop); `engine/creation.py::_match_attribute` prefix-matched Force attrs in the freeform wizard (excluded). New `tests/test_chargen_hardening_2026_07_03.py` (13). Targeted gate: 397 chargen-adjacent tests + foundation smoke green.
- **`fable-addendum-hygiene`** (**`5c215b5`** + **`f2cfeeb`**, two independent sessions converged on the same spec section — see Process notes) — `deepscan`/`scan` help-alias cross-platform collision fixed (sole ownership + `see_also` cross-ref), `engine/help_loader.py::iter_help_files` now sorts `dirnames` for deterministic `os.walk` traversal, a new alias-dup ratchet surfaced ~32-34 pre-existing unrelated collisions (logged `HYG.help_alias_collision_backlog`, out of scope), a new `serial` pytest marker (`pytest.ini` + `tests/conftest.py`) skips 3 flaking jsdom/Node-subprocess SPA test modules under xdist workers, and the Phantom Tonnage 36th-arc skill-uniqueness relaxation was recorded explicitly (npc-yaml header + test docstring) as a sanctioned Fork-B one-off, not silent precedent.
- **`4aecb7a`** — bookkeeping correction: the reconciling session's TODO.json note claimed an `xdist_group` marker shipped; verified at HEAD (by invariant-auditor at `8b24d69`) that no such marker exists — the second flaky-test trio rides the landed `pytest.mark.serial` mechanism instead. TODO.json note corrected to match reality.
- **`59684a3`** ("attended-bookkeeping") — Qwen §2's one-curl install-time check run against Brian's live Ollama daemon: **Ollama 0.31.1, `qwen3.5:9b` — `format:"json"` IS honored** (closes `AI.QWEN_JSON_FORMAT_CURL_VERIFICATION`; two observations recorded: the grammar guarantees valid JSON but not the asked-for bare-array shape, and `thinking` is ON by default on this Ollama/model combo, contra the spec's off-by-default read — flagged under `AI.QWEN_ERA_DROP_TELEMETRY`). Two stale pending design-calls (`UX.living_sheet_delta_semantic`, `FUN.shop_verb_and_natural_verb_surface`) moved to `design_calls_resolved_recent` — both had already been decided by Brian on 2026-07-01 per the fun16 CHANGELOG entry; this was bookkeeping cleanup, not a new ruling.
- **Loops.** `SWMUSH-OpusLoop` and `SWMUSH-EventsOvernight` are DISABLED per Brian for the attended session — live-verified via `Get-ScheduledTask` at handoff time: both still show `State: Disabled`. Re-arm via `Enable-ScheduledTask` once Brian's full gate is done and the attended session is closed out.

## 4. The centerpiece: the Fable §2 signal-producer audit

Fable's addendum (`docs/fable/FABLE_ADDENDUM_FOR_CLAUDE_CODE_2026-07-03.md` §2) named the pattern behind the staged-cult zero-payout bug Fable's own review missed: *"for any distributor-gated pipeline, audit the signal producer on every path variant, not the distributor — presence ≠ wiring, existence ≠ validity."* It listed **6 candidate reward pipelines** for a pre-launch one-pass audit: territory contest scoring, bounty split/claim, org payroll, communal title-share, chain `slot_totals`, grind daily-cap counters.

The audit pass (recon → break-it against real production seams → adversarial re-verification of every finding against HEAD before fixing) returned **12 CONFIRMED findings (F1–F12), 12/12, 5 of them blockers**, fixed via 4 conflict-partitioned parallel worktree lanes, all landed same day:

- **`a3da2d0`** (F12, mob-grind daily-cap) — `engine/hunting_rewards.py::on_huntable_kill` booked the daily-cap counter by the nominal pre-throttle reward while `db.adjust_credits` scales a positive faucet delta by the live `@economy` throttle; a throttled/suppressed faucet let the cap engage on income the player never received. Fixed: books `applied = max(0, new_balance - credits_before)` (read via `db.get_character` immediately before `adjust_credits`) instead of the nominal `reward`. New `tests/test_hunting_rewards_faucet_throttle_cap.py` (5).
- **`5f2e9f1`** (Lane C: F7/F6/F3, faction economy) — **F7** (major): `@faction treasury add` silently no-op'd on a typo'd org code (resolved to id=0, printed a fake success). **F6** (blocker, Brian-ruled): faction treasury had no real gameplay faucet — shipped `engine.territory.tick_territory_income` (org treasury += total zone influence × `territory.income_per_influence_point`, new `data/tunables.yaml` key, **default 7cr/influence-point/day**), called from inside the existing `faction_payroll_tick` (same ~1-day cadence, no new scheduler entry), landing before that tick's stipend sink. Paired with a documented-but-missing member `faction donate <amount>` faucet (`engine.organizations.donate_to_treasury`, Guide_10 §209): debit-first/refund-on-failure, capped weekly rep (1 rep/100cr, cap 20/week) via `adjust_rep`. **F3** (major): the two highest-volume influence producers (`faction invest`, hourly presence tick) never triggered contest auto-declare because both are zone-keyed and pass no `region_slug`; fixed inside `adjust_territory_influence` itself via a new `get_regions_for_zone` helper. New `tests/test_audit_fix_faction_economy_2026_07_03.py` (13).
- **`caea595`** (Lane B: F8/F9/F10, staged-communal) — **F8/F9** (MAJOR×2): a resolved staged-cult stage anomaly could be silently discarded by a region prune (hourly tick or a player's `anomalies` command) before the communal orchestrator's poll consumed it — on the FINAL stage this lost the entire multi-stage win (no rep/title/1000cr capstone/relic). Fixed via a `WildernessAnomaly.scenario_hold` prune-exemption flag + explicit idempotent consumption (`WA.delete_anomaly_by_id`), plus wiring `WA._dispatch_site_cleared`'s inline push-notify into the skill-resolution path (`_resolve_anomaly_skill`) where it was verified NOT actually wired despite its own docstring's claim. **F10** (MINOR, Brian-ruled): extended the same participant fan-out established for the `site_cleared` chain hook (archetype Fork-4B) to communal staged-cult rewards — the resolver keeps full `stage_clear_points()`, every other participant earns a reduced share via new `communal.staged_participant_share` (`data/tunables.yaml`, **default 0.5**), all through the existing `contributions_json` ledger. New `tests/test_audit_fix_staged_communal_prune_race_2026_07_03.py` (4).
- **`58371f6`** (Lane A: F1/F11/F4/F5/F2, combat-victory/bounty/PvP-influence) — **F1** (BLOCKER): a straight-to-DEAD kill skipped ALL victory hooks (rep, `territory.on_npc_kill`, Region-Anchor contest resolution) because `_cleanup()` pops a DEAD combatant before the victory-branch scan runs; fixed by unioning with the pre-resolution `_newly_dead` snapshot. **F11** (BLOCKER): chain `combat_won` credit was lost when a tagged foil died in a non-final combat round; fixed via a per-`CombatInstance` accumulator (`_accumulate_chain_defeated_this_round`/`_fire_chain_combat_won`) shared by both the normal auto-resolve path and the admin `ResolveCommand`, which previously never fired the chain hook at all. **F4** (BLOCKER): `engine.bounty_board.notify_target_killed` reassigned `contract.claimed_by` to whoever landed the killing blow — a claim hijack; fixed by paying `contract.claimed_by` unconditionally. Logged `BOUNTY.cooperative_kill_split_reward` as a genuine balance fork rather than guessing. **F5** (MAJOR, Brian-ruled): bounty payout now gates on wound ≥ INCAPACITATED(4) instead of ≥ MORTALLY_WOUNDED(5) (`captured_alive = wound < DEAD`), matching where combat itself actually ends. **F2** (BLOCKER): `engine.territory.on_pvp_kill` had zero production callers; wired into `engine/death.py::on_pc_death` at the point `_record_pvp_death_and_loot_factor` already uses `killer_id`. New `tests/test_audit_fix_combat_victory_2026_07_03.py` (10).

All five fixes route through existing funnels (`adjust_credits`/`adjust_rep`/`adjust_territory_influence`) — no schema change, no new faucet/sink shape, era-clean, `force_sensitive` untouched across all four lanes.

## 5. The aggregate verify-drop — one MAJOR found and fixed

A 4-agent verification fanned out over the merged range `4aecb7a..a54b4bd` (all four audit-fix lanes together):

- **invariant-auditor:** CLEAN, 1 WARN (the F10 lane-B CHANGELOG entry's wording was ambiguous on whether the participant share was additive or a split of a fixed pool — clarified in `89772ce`'s CHANGELOG addendum: it's ADDITIVE, payout scales with participant count by design, per Brian's Fork-4B ruling).
- **test-runner:** 1,794/1,795 passed, 0 failed (one unrelated environment skip).
- **smoke-verifier:** 226/227 boot-clean; the one failure is the pre-existing `tests/smoke/test_smoke_telnet.py::test_t2_telnet_movement` order-dependent isolation flake (re-verified passing solo both times) — now logged as `TEST.telnet_movement_order_dependent_flake`.
- **code-reviewer:** 1 MAJOR, fixed in the follow-up drop `audit-fix-pvp-dedup` (**`89772ce`**): `_apply_combat_wear`'s `wl==0` victory block had no `is_npc` filter — a beaten PC fired the NPC-flavored `adjust_rep`/`territory.on_npc_kill` AND stacked with lane A's new `on_pvp_kill` at death, double-counting the flagship contest-PvP influence — AND the whole block re-fired every round a beaten-but-alive opponent lingered short of DEAD (farmable). Fixed with a **two-tier `(victor, opponent, tier)` per-fight latch** (`combat._victory_credited`, mirroring F11's `_chain_credited_ids` pattern): "beaten" ≥ INCAPACITATED gates log/rep/influence/spacer with PC opponents excluded from the NPC-flavored rep/influence pair (PC defeats award exclusively via `on_pvp_kill`); "killed" ≥ MORTALLY_WOUNDED gates the Anchor-contest/dsp-hunter/creature-spoils loops (now also running on rounds with no newly-beaten opponent), so a Region Anchor first credited at INCAPACITATED still resolves its contest on death. Also ceiling-clamped `engine/hunting_rewards.py`'s `applied` delta to the nominal reward (a foreign concurrent credit inside the read gap can no longer inflate the grind daily-cap meter). New `tests/test_audit_fix_pvp_dedup_2026_07_03.py` (5); 152-test targeted sweep green.

## Landed-to-main ledger (commit hashes, chronological)

| Time | Commit | Drop |
|---|---|---|
| 09:39 | `80bed81` | bounty-track-skill-selection |
| 09:43 | `e00d07c` | CHARGEN hardening (3 blockers + 1 minor) |
| 10:02 | `646e26a` | staged-questline-archetype-slice1 ("The Undertow Skim") |
| 10:13 | `2966527` | capital-ship-combat-bridge (recovered + hardened) |
| 10:16 | `59684a3` | attended-bookkeeping (Qwen §2 curl PASS + stale-call cleanup) |
| 10:28 | `19fc04e` | Merge: keep-both on the completion-type seam |
| 10:59 | `5c215b5` | Fable addendum §4/§5 hygiene (session A) |
| 11:05 | `15159b3` | Handoff + design specs commit (overnight docs) |
| 11:24 | `f2cfeeb` | fable-addendum-hygiene (session B, same spec — see Process notes) |
| 11:51 | `4aecb7a` | bookkeeping: TODO.json phantom-claim correction |
| 13:05 | `a3da2d0` | audit-fix-grind-cap (F12) |
| 13:15 | `5f2e9f1` | audit-fix-faction-economy (F7/F6/F3) |
| 13:19 | `caea595` | audit-fix-staged-communal (F8/F9/F10) |
| 13:34 | `58371f6` | audit-fix-combat-victory (F1/F11/F4/F5/F2) |
| 13:38 | `a54b4bd` | Merge: all 4 audit-fix lanes onto origin/main |
| 14:19 | `89772ce` | audit-fix-pvp-dedup (verify-drop follow-up; HEAD) |

## Pending Brian

- **Full `run_all_tests.bat` gate.** Every drop today landed on a targeted-gate-green basis (no commit in today's range reports an actual full-suite run — `run_all_tests.bat` appears in today's CHANGELOG diffs only in descriptive/reference context, never as a completed run's result line). A clean serial full-suite run on `89772ce` is still owed before calling the day's work final, per the standing drop-workflow gate.
- **Balance tunables to eyeball** (all Brian-ruled defaults, conservative per his posture, worth a sanity look once the economy has run a few in-game days): `territory.income_per_influence_point` = **7cr/influence-point/day** (`data/tunables.yaml`, F6); the member `faction donate` weekly rep cap = **20 rep/week** (1 rep/100cr, Guide_10 §209); `communal.staged_participant_share` = **0.5** (additive — total staged-cult payout scales with participant count by design, F10).
- **New design fork: `BOUNTY.cooperative_kill_split_reward`** (`design_calls_pending_brian`, OPEN) — should a non-claimant who lands the killing blow on someone else's claimed bounty target get a split of the reward, or does the claimant always get 100% (the shipped, invariant-safe default)? Surfaced by F4's fix; not decided here.
- **Standing deferred capital-ship tiers** (`design_calls_pending_brian`, both `PENDING BRIAN`, pre-dating today but still open at HEAD): `SPACE.capital_disable_board_tier` (disable/board win-condition — needs an NPC-ship "disabled" state, a board-a-crippled-capital verb, and Brian's threshold/reward call) and `SPACE.capital_crewed_setpiece_tier` (a richer crewed multi-gunner capital set-piece / escort-screen target).
- **Still-open from today's other drops:** `BOUNTY.track_third_skill_phantom` (the third BountyTrack investigation skill, `"tracking"`, is unregistered and can never win the ranking — which real skill belongs there?).

## Deferred / logged (TODO.json, not blocking)

- `TEST.telnet_movement_order_dependent_flake` (`tech_debt`) — `test_t2_telnet_movement` fails under full-suite ordering (combat state leaking into a fresh telnet login) but passes solo/isolated; not a production bug, keeps the accepted-red arithmetic honest. Fix shape: find the leaking combat smoke test and/or reset the combat-manager singleton between smoke classes.
- `PERF.presence_tick_zone_regions_memo` (`tech_debt`, low severity) — the F3 branch's `get_regions_for_zone` re-runs once per (zone, org) pair on the hourly presence tick; a cheap indexed join today, memoizable per-tick later.
- Qwen §6/§7 (era-drop telemetry + acceptance cycle) remain logged as deferred tech debt (`AI.QWEN_ERA_DROP_TELEMETRY`, `AI.QWEN_SWAP_OUT_OF_SCOPE_BATCH`) — unchanged today; §2's curl check itself is now closed (`59684a3`).
- `HYG.help_alias_collision_backlog` (`tier_2_queued`) — ~34 pre-existing help-corpus alias collisions surfaced by today's new alias-dup ratchet, out of scope for the hygiene drop that found them.
- `QUEST.staged_archetype_fast_follows` (`tier_2_queued`) — Fork 8A (move `SCENARIO_TEMPLATES` from a Python module literal to a YAML loader), Fork 6A (unify the 5 staged cults onto the archetype), Fork 9C (end-game clears-gated reward remix). Deliberately deferred by `staged-questline-archetype-slice1`.

## Process notes / gotchas

- **The detached-gate implementer stall pattern, again — and its resume fix.** The capital-ship bridge stalled uncommitted in the live tree while its own bookkeeping had already swept to HEAD via an unrelated commit (`6be07ad`) — the same failure class the overnight handoff logged for the reward-fix/space-combat drops. The resume discipline that worked again: **"block on your existing gate task synchronously, don't relaunch, commit+push in-turn."**
- **Multi-session same-spec collision on the addendum drop.** Two independent sessions both implemented `FABLE_ADDENDUM_FOR_CLAUDE_CODE_2026-07-03.md` §4/§5 concurrently: `5c215b5` ("Fable addendum §4/§5 hygiene") landed first, then `f2cfeeb` ("fable-addendum-hygiene") landed the same spec section again shortly after (11:24 vs 10:59) — both against real HEAD, both with their own test files and CHANGELOG/TODO entries, requiring a merge (`8b24d69`) and a follow-up bookkeeping correction (`4aecb7a`) once the reconciled tree's actual mechanism (`pytest.mark.serial`, not `xdist_group`) was re-verified. Worth a coordination note (a claim-lock or an explicit "who owns §4/§5" ping) before dispatching the same spec section to two lanes again.
- **Keep-both merge discipline on the completion-type seam held.** The overnight handoff's MERGE WATCH called the collision (`site_cleared` vs. `space_combat_won` both touching `ALLOWED_COMPLETION_TYPES`/`chain_events.py`/`chains.yaml`/the T5 walker) in advance; `19fc04e` resolved it correctly — both survive, `chains.yaml` was reconstructed by hand (base + both insertions) rather than trusting the textual 3-way merge, which had interleaved the two appended chains. This is the reusable playbook for any future two-lane addition to a shared enum/registry seam.
- **The lane-B agent's self-catch on `_dispatch_site_cleared`.** While fixing F8/F9, the staged-communal lane re-verified at HEAD (rather than trusting its own docstring) whether `WA._dispatch_site_cleared` was actually wired into the skill-resolution path (`_resolve_anomaly_skill`) — and found it was NOT, despite the docstring's claim that it fired on "every resolution path." Caught and fixed inline as part of the same drop. A concrete instance of the no-phantom-claims discipline catching a real gap mid-fix rather than after.
- **Break-it beat static review for reward pipelines, again — 12/12 confirmed.** Every one of the audit's 12 findings (F1–F12) was reproduced against a REAL production seam (real `Database(":memory:")`, real command/hook call sites, no hand-injected `contribs`/`on_combat_won`/`on_pvp_kill` calls) before being counted as confirmed, matching Fable's own §2 lesson from the staged-cult zero-payout miss ("presence ≠ wiring, existence ≠ validity"). All 12 confirmed, all 12 fixed, zero false positives reported back — reinforces treating break-it as the primary gate for any distributor-gated reward pipeline, not the confirmation step.

## UNVERIFIED / not independently confirmable

- The launching task described the audit as a "24-agent workflow." I could not ground a specific agent-count anywhere in `git log`/`git show`/TODO.json/CHANGELOG — that's session-internal orchestration detail, not visible in the diff. Described the process qualitatively (recon → break-it against real producers → adversarial re-verification) instead, which every audit-fix commit message explicitly documents; the main session should confirm or drop the specific headcount if it's asserted elsewhere.
